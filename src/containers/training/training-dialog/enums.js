export const TestState = Object.freeze({
    ENTER: 'enter',
    LEAVE: 'leave',
    SELECT: 'select',
    SHOW_RESULT: 'showResult',
    HIDE_OLD_TEST: 'hideOldTest'
});

export const VariantState = Object.freeze({
    DISABLED: 'disabled',
    SELECT: 'select',
    WRONG: 'wrong',
    RIGHT: 'right'
});